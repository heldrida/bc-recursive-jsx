import styled from 'styled-components'

const SectionItemContainer = styled.div`
  margin-left: 2rem;
  margin-top: 3rem;

  & > h3 {
    padding-bottom: 1rem;
  }
`

export {
  SectionItemContainer
}